{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "[ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ] && exit 0; [ \"$VERCEL_GIT_COMMIT_REF\" = \"dev\" ] && exit 0; [ -z \"$VERCEL_GIT_PULL_REQUEST_ID\" ] && exit 0; exit 1"
}
